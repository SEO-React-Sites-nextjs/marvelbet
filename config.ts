const config = {
    baseUrl: "https://www.marvel365bet.com",
    siteName: "MARVELBET", // 基本上放站點清單中的主關鍵字(AI欄位)(AI是column代碼不是指AI)
    articleAuthor: 'MARVELBET', // 基本上放siteName
    publisher: 'MARVELBET', // 基本上放siteName
    locale: "en_BD", // 例如 en_US、en_GB"，不知道的話看看layout裡的metadata有沒有
    language: "en" // 如果不是英文，記得要改
}

export { config }